class Job:
    id: int
    deadline: int
    processingTime: int

PriorityQueue jobs (sorted by deadline)

jobs.add(Job1)
jobs.add(Job2)
jobs.add(Job3)

while not jobs.empty():
    job = jobs.poll()
    print("Executing job", job.id, "deadline:", job.deadline)
