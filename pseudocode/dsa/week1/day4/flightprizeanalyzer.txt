class Flight:
    price: int
    duration: int

function compare(f1: Flight, f2: Flight):
    if f1.price != f2.price:
        return f1.price - f2.price
    else:
        return f1.duration - f2.duration

function mergeSort(flights: List<Flight>):
    if size <= 1: return flights
    mid = size/2
    left = mergeSort(left half)
    right = mergeSort(right half)
    return merge(left, right, compare)

Main:
    flights = [Flight(5000, 3), Flight(4000, 4), Flight(5000, 2)]
    sortedFlights = mergeSort(flights)
    print(sortedFlights)
