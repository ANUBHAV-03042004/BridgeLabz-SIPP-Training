class Book {
    String title;
    int year;
    double rating;
}

List<Book> books = Arrays.asList(
    new Book("Java Basics", 2018, 4.7),
    new Book("C++ Primer", 2012, 4.2)
);

books.stream()
     .filter(b -> b.year > 2015 && b.rating > 4.5)
     .forEach(b -> print(b.title));
